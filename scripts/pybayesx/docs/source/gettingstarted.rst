Getting Started
===============

============
Installation
============

PyBayesX can be installed with `pip -e scripts/pybayesx`.
The `-e` flag makes this an editable install, so changes to the source will be avaliable without reinstallation